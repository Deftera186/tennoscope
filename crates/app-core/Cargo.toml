[package]
name = "app-core"
version = "0.1.0"
description = "Application core wiring acquisition, storage and health into views"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
# Internal workspace crate: the shipped artifact is the TennoScope bundle, not a crates.io release.
publish = false

[dependencies]
serde.workspace = true
thiserror.workspace = true
local-store = { path = "../local-store" }
warframe-domain = { path = "../warframe-domain" }
warframe-acquisition = { path = "../warframe-acquisition" }

[dev-dependencies]
serde_json = "1"
tempfile = "3"
